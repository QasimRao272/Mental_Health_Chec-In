<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.google.mentalhealthchec_in">

    <application
        android:allowBackup="true"
        android:icon="@drawable/final_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WhatHveYouBeenUpToActivity"></activity>
        <activity android:name=".HowAreYouActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CustomizedActivity" />
        <activity android:name=".HelpUsImprove" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>